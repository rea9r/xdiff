# sample workspace

This directory mirrors a small project layout used by the folder
compare fixture. The README is intentionally identical on both
sides so it appears as `same`.

## Layout

- `src/` — application sources
- `api/` — JSON payloads
- `docs/` — markdown documentation
- `nested/deep/` — deeper hierarchy for scroll testing
