openapi: 3.0.3
info:
  title: Basic Fixture API (New)
  version: 1.0.0
paths:
  /users:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        '201':
          description: created
        '200':
          description: changed response type
          content:
            application/json:
              schema:
                type: array
